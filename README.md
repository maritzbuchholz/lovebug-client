# Lovebug-client

Front-end wireframe the Lovebug project - a compatibility dating app. Components were built using Antigravity.

## Quick Start

Clone the repo locally and install the dependencies by running the command below:

```bash
npm install
npm run dev
```